import styled from "styled-components";
import DefaultPage from "../../components/DefaultPage";


const dataset = [
]

const Conditions = () => {
  return (
    <DefaultPage title="Conditions" desc="What Conditions do we  have?" dataset={dataset}>
      <img src="" />
    </DefaultPage>
  );
};
export default Conditions;
